# bugfix.md — сводный аудит багов stalbot

> Составлено мульти-агентным ревью: 8 параллельных агентов (`agent-skills:code-reviewer` × 6,
> `agent-skills:security-auditor` × 1, `agent-skills:test-engineer` × 1), каждый явно прогнал
> процесс/verification-шаги скиллов `code-review-and-quality`, `debugging-and-error-recovery`,
> `security-and-hardening`, `test-driven-development`. Покрыт весь `src/stalbot/**` (все слои)
> плюс `pyproject.toml`, `Dockerfile`, `docker-compose.yml`, `deploy/stalbot.service`,
> `scripts/backup.sh`, `.gitignore`, git-история.
>
> **Только анализ — код не менялся.** Формат каждой записи: ID · Файл+строки · Описание ·
> Severity (critical/high/medium/low + Nit/Optional/FYI) · Кто выявил · Предлагаемый фикс ·
> Regression-тест нужен? · Зависимости.
>
> Дубликаты, найденные независимо разными агентами, помечены «⚡ CLUSTER» — это сходится
> само по себе является сильным сигналом, что баг реальный.

---

## ⛔ Критично + безопасность (блокирует всё остальное — stop-the-line)

### ⚡ CLUSTER-1 — Гонка при подтверждении сделки: возможна двойная запись строки в Тикеты
**Найдено независимо 4 агентами: APP-1 (application), TICK-1 (tickets), SEC-3 (security), TEST-1 (test-coverage)**

- Файлы:
  - `src/stalbot/application/services/transactions.py:86-154` (`register`, `_replay_if_seen`, `_find_free_row`)
  - `src/stalbot/infrastructure/cache/repositories/idempotency.py:17-42` (`get`/`record`, `INSERT OR IGNORE`)
  - `src/stalbot/infrastructure/sheets/client.py:105-173` (`batch_update`/`write_verified` — лок держится только вокруг самого `batch_update`, read-back верификация уже вне лока)
  - `src/stalbot/presentation/cogs/tickets/cog.py:571-634` (`_confirm_precheck`, `_on_confirm_button`, `_on_amount_submitted`)
- Описание: `_replay_if_seen()` читает `IdempotencyRepository.get(key)` без блокировки; ключ записывается только **после** завершения записи в Sheets (`idempotency.record()`). Два конкурентных вызова с одним и тем же ключом (двойной клик «✅ Подтвердить», два админа одновременно, гонка `_find_free_row` при двух разных `/add`) оба проходят replay-проверку до того, как первый успел записать — оба вычисляют одну и ту же «свободную» строку (`_find_free_row`, тоже без лока) и оба пишут. Второй `write_verified` затирает данные первого; read-back первого видит расхождение → компенсация «очистить A-E,H» стирает **уже корректно записанные данные второго**. Дополнительно: `is_first_transaction` (referrer → колонка `H`) может сработать `True` в обоих конкурентных вызовах для нового игрока → двойной счёт реферала через `СЧЁТЕСЛИ($H:$H)` в колонке `P`.
- Severity: **critical** (реальная гонка в единственном источнике правды — торговом реестре; деньги/игровая валюта могут быть потеряны или задвоены)
- Кто выявил: `agent-skills:code-reviewer` (APP слой, TICK слой) через `debugging-and-error-recovery` (root-cause трассировка гонки) + `agent-skills:security-auditor` (SEC-3, тот же баг с фокусом на «real-money ledger») + `agent-skills:test-engineer` (TEST-1: подтвердил, что ни один тест не проверяет настоящую конкурентность — только последовательные вызовы)
- Предлагаемый фикс: держать **один и тот же** `asyncio.Lock` (по каналу тикета / по `idempotency_key`) на весь путь find-row → write → verify → record, а не только вокруг сетевого вызова. Простейший вариант — сериализовать весь `TransactionService.register()` одним грубым локом (пропускная способность admin-команд низкая, это приемлемо).
- Regression-тест нужен: **да** — `asyncio.gather(register(req), register(req))` с одинаковым `idempotency_key` против fake `SheetsClient`; assert `write_verified` вызван ровно один раз и обе строки не сталкиваются. Отдельный тест на конкурентную первую-сделку двух новых игроков с одним рефералом → `H` пишется один раз.
- Зависимости: фикс в `TransactionService`/`SheetsClient` автоматически закрывает и TICK-1 (UI-сторона гонки в `cog.py`), но там дополнительно нужен `_confirm_precheck`, перечитывающий статус **прямо перед** записью, а не только при показе модалки.

---

### INFRA1-1 — `ensure_writable()` пропускает проверку защищённых колонок при «перевёрнутом» диапазоне (fail-open)
- Файл: `src/stalbot/infrastructure/sheets/protection.py:43-51`
- Описание: `for index in range(start, end + 1)` — если каким-то образом `end &lt; start` (например, диапазон вида `"DataBase!G3:F3"`), `range()` в Python пуст, и **ни одна колонка не проверяется** — запись в формульную колонку (F/G/J-P/R/S) уходит в сеть без единственного заявленного «fail loudly and locally» барьера.
- Severity: **critical** — нарушает абсолютный инвариант проекта («бот никогда не пишет формулу») в единственной точке его принудительного соблюдения.
- Кто выявил: `agent-skills:code-reviewer` (sheets/cache) через five-axis review (edge case на границах колонок, именно то, что просили проверить)
- Предлагаемый фикс: `lo, hi = sorted((start, end)); range(lo, hi + 1)`, либо явный `ValueError` на некорректный/перевёрнутый диапазон вместо молчаливого принятия.
- Regression-тест нужен: **да** — `ensure_writable("DataBase!G3:F3")` должен поднять `ProtectedRangeWriteError`. Сейчас не поднимет.
- Зависимости: нет.

---

### DOM-1 — `evaluate_amount` тихо портит точные денежные суммы через обязательный float-роундтрип
- Файл: `src/stalbot/domain/money.py:207-253`
- Описание: числовые токены переформатируются в текст и повторно парсятся через `ast.parse` — но в грамматике Python нет литерала fixed-point, поэтому любой токен с точкой становится `float` (IEEE-754) до того, как `_eval_node` конвертирует его обратно в `Decimal(str(...))`. Круглый путь точен только пока значение помещается в ~15-17 значащих цифр float64.
  ```python
  evaluate_amount("999999999999999.99")  # -> Decimal('1000000000000000.0')  НЕВЕРНО, скачок на порядок
  ```
  Достижимо на бытовых суммах в диапазоне квадриллионов — легко получить через множитель `ккк` (×10⁹) на скромной базе. Прямое нарушение задокументированного инварианта «money must ALWAYS be Decimal, never float» — как раз для функции, которая обслуживает `/add`, подтверждение тикета, `/setprice`, `/setboost`, `/new_price`, итоги заказа бустов.
- Severity: **critical** — тихая порча денежных данных, достижимый ввод, прямое нарушение задокументированного инварианта; проскочило мимо 113+ существующих тестов и hypothesis-свойства (они не гоняют высокоточные дробные литералы именно через AST-путь).
- Кто выявил: `agent-skills:code-reviewer` (domain) через five-axis correctness + целевой поиск «floating point creeping in»
- Предлагаемый фикс: не пропускать десятичные литералы через нативную грамматику `ast.parse`. Заменять каждый распознанный денежный токен плейсхолдер-идентификатором (`__m0__`), привязанным к предварительно распарсенному `Decimal` в отдельной таблице; `ast.Name` резолвится через lookup, а не через `ast.Constant.value`.
- Regression-тест нужен: **да** — `evaluate_amount("123456789012345.6789") == Decimal("123456789012345.6789")` (сейчас падает), плюс кейс `.99`, скачивающий на порядок.
- Зависимости: независим от DOM-3 (другая причина/порог), но оба трогают один и тот же блок кода — логично чинить в одном заходе.

---

### APP-2 — `int()`-усечение при записи в Sheets расходится с `Decimal`, закэшированным/показанным пользователю
- Файлы: `application/services/transactions.py:105-116,167`; `pricing.py:89-96,144-150,281-287`; `catalog.py:86-98,168-177`
- Описание: запись в Sheets везде идёт через `int(request.amount)` (усечение к нулю, не округление), а закэшированный/возвращённый объект использует **неусечённый** `Decimal`. В `TransactionService.register()` это означает, что пользователь видит в эфемерном подтверждении одно число, а в реестре оказывается другое (усечённое). В `PricingService`/`CatalogService` расхождение вообще не самокорректируется последующим синком (в отличие от `transactions.py`, где синк хотя бы частично лечит кэш) — рассинхрон кэша и таблицы держится до случайного полного ресинка. Также игнорируется собственная конвенция округления `domain/money.py::format_amount` (`ROUND_HALF_UP`).
- Severity: **critical** (transactions.py — видимое пользователю расхождение реестра/подтверждения), **high** (pricing/catalog — тихий, несамокорректирующийся дрейф кэша)
- Кто выявил: `agent-skills:code-reviewer` (application) через five-axis correctness, сверка 5 мест записи с одинаковым паттерном
- Предлагаемый фикс: единая функция `round_for_storage(value: Decimal) -> Decimal` в `domain/money.py` с той же конвенцией `ROUND_HALF_UP`, что и `format_amount`; каждое место записи использует ровно это округлённое значение и для payload в Sheets, и для объекта, который кэшируется/возвращается — никогда два независимо выведенных значения одного поля.
- Regression-тест нужен: **да** — вызов каждого из 5 мест с дробным `Decimal` (напр. `Decimal("150.5")`), assert записанное в Sheets и закэшированное/возвращённое значение совпадают.
- Зависимости: все 5 мест могут чиниться одним общим хелпером за один проход.

---

### INFRA1-2 — Ложное несовпадение при read-back верификации может стереть корректно записанную строку
- Файл: `src/stalbot/infrastructure/sheets/client.py:159-173` (`write_verified`/`_matches`)
- Описание: сравнение `result.get(ref, []) == list(values)` — строгое сравнение вложенных списков. Google Sheets API документированно обрезает висячие пустые ячейки/строки при чтении (не дополняет обратно). Строка, оканчивающаяся пустой строкой (частый случай — колонка `H`/«Пришёл от» пуста при сделке без реферала), почти наверняка вернётся короче отправленной → **ложное несовпадение** → компенсация стирает только что корректно записанную строку и поднимает `SheetsWriteConflictError` пользователю на успешно записанные данные.
- Severity: **critical/high** — ложное срабатывание сети безопасности может уничтожить корректно записанные данные реестра; тот же класс бага («API-квирк ломает сопоставление»), что автор уже дважды ловил раньше в этом же клиенте.
- Кто выявил: `agent-skills:code-reviewer` (sheets/cache) через `debugging-and-error-recovery` (паттерн совпадает с задокументированным историческим багом `batch_get`)
- Предлагаемый фикс: сравнивать по ячейкам с right-padding более короткой (read-back) строки/грида до формы записанной, трактуя отсутствующую висячую ячейку как равную явно записанной `""`.
- Regression-тест нужен: **да** — `write_verified` со строкой, оканчивающейся `""`, где мокнутый `values_batch_get` возвращает её без висячей пустой ячейки — не должен ни стирать, ни поднимать исключение. Существующие тесты `test_write_verified_*` используют только непустые скалярные ячейки.
- Зависимости: нет напрямую, но общий корень с задокументированным историческим квирком `batch_get`.

---

### PRES-1 — Глобальный error-handler утекает внутренние сообщения Sheets-исключений пользователю Discord
- Файл: `src/stalbot/presentation/errors.py:62-80`
- Описание: `SheetStructureError` (client.py:318, текст вида `"missing sheet: 'Users'; block 'DataBase' (A1:H1) header mismatch: expected (...), found (...)"`) и `ProtectedRangeWriteError` (protection.py:48, текст вида `f"column {letter} on sheet ... is formula-owned... (attempted write to {ref!r})"`) — оба `StalbotError`, но отсутствуют в `_DOMAIN_MESSAGES`. `_resolve_message` в этом случае откатывается на `str(cause)` — сырой внутренний текст показывается прямо в error-embed пользователю. Прямое нарушение инварианта «никогда не показывать сырое исключение, только trace_id». Тот же пробел у `domain.errors.PermissionError` (сейчас без райзеров, но подвержен той же дыре).
- Severity: **critical** (Correctness + Security/information-disclosure; путь подтверждённо достижим, не гипотетичен)
- Кто выявил: `agent-skills:code-reviewer` (presentation) через `security-and-hardening` (утечка информации на границе доверия) + `code-review-and-quality` (немаппленный exception-путь)
- Предлагаемый фикс: в `_resolve_message` откатываться на `str(cause)` только для `DomainError` (сообщения которого курируются как user-facing по конвенции); для `InfrastructureError`/всего немаппленного — всегда общее сообщение + trace_id, как в ветке «неизвестное исключение».
- Regression-тест нужен: **да** — прогнать `SheetStructureError("missing sheet: 'Users'")` через handler, assert текст embed не содержит `"Users"`/`"missing sheet"`.
- Зависимости: нет.

---

## Domain (`src/stalbot/domain/**`)

**DOM-1** — см. блок «Критично» выше.

**DOM-2 / ⚡ дублирует SEC-2** — `parse_deadline`: необработанный `OverflowError` от простого текстового поля Discord
- Файл: `src/stalbot/domain/clock.py:124-125,180-191` (`_RELATIVE_HOURS_RE`/`_RELATIVE_MINUTES_RE`, `_parse_relative_hours`/`_parse_relative_minutes`)
- Описание: `int(match.group(1))` не ограничен по числу цифр, передаётся в `timedelta(hours=...)`/`timedelta(minutes=...)`. Достаточно большое число переполняет внутреннее C-int представление `datetime` → `OverflowError`, не пойманный нигде выше по стеку. Поле заполняется **обычным игроком** (автором тикета) в модалке заказа бустов, помещается в лимит `max_length=50` (`"через 999999999999999999999999999999 часов"` — 42 символа). Модалка не проходит через общий `on_app_command_error` (это `discord.ui.Modal.on_error`, у которого нет обработчика) — падение тихое, без ответа пользователю, без trace_id в логах.
- Severity: **high** — необработанный краш от непривилегированного пользовательского ввода, нарушает собственный контракт иерархии исключений проекта.
- Кто выявил: `agent-skills:code-reviewer` (domain, DOM-2) независимо от `agent-skills:security-auditor` (SEC-2, тот же баг с фокусом на непривилегированный доступ и отсутствие error-хендлинга в модалках)
- Предлагаемый фикс: ограничить число цифр в регулярке (`(\d{1,6})`) или поймать `OverflowError`/`ValueError` вокруг `timedelta(...)` и переподнять как `DeadlineParseError`.
- Regression-тест нужен: **да** — `parse_deadline("через 999999999999999999999999999999 часов", now=...)` должен поднять `DeadlineParseError`.
- Зависимости: связан с SEC-4 (общий пробел — у Modal вообще нет `on_error`).

**DOM-3** — `parse_amount`/`evaluate_amount`: тихая потеря точности выше 28 значащих цифр (глобальный `Decimal`-контекст без явной области видимости)
- Файл: `src/stalbot/domain/money.py` (`_parse_number_token`, `_apply_binop`)
- Описание: нигде не задаётся явный `decimal.Context`/`localcontext()` — арифметика идёт в контексте по умолчанию (28 значащих цифр, `Rounded`/`Inexact` trap выключены) → тихое округление вместо исключения при вводе ~20+ цифр с множителем.
- Severity: **medium** — редко достижимо на практике, но реальное нарушение инварианта; также архитектурный риск (глобальный контекст может быть изменён другим кодом).
- Кто выявил: `agent-skills:code-reviewer` (domain)
- Предлагаемый фикс: `with decimal.localcontext() as ctx: ctx.prec = 60` вокруг парсинга/вычисления, с запасом выше реалистичного максимума при лимите строки в 256 символов.
- Regression-тест нужен: **да**.
- Зависимости: независим от DOM-1, но чинить логично в одном заходе (одна функция).

**DOM-4** — `format_compact`: отображение в неверной единице на границе округления
- Файл: `src/stalbot/domain/money.py:297-324`
- Описание: `format_compact(Decimal("999950"))` → `"1000 к"` вместо `"1 кк"` — единица выбирается по «сырой» величине, округление квотиента происходит уже после выбора суффикса.
- Severity: medium (UX/корректность отображения, легко достижимо у любого игрока рядом с круглой цифрой).
- Кто выявил: code-reviewer (domain)
- Фикс: после квантования проверять, не достиг ли числовой квотиент 1000, и если да — переэскалировать суффикс.
- Regression-тест: да.
- Зависимости: нет.

**DOM-5** — `parse_sheet_datetime`: принимает некорректный 3-значный год вместо `None`
- Файл: `src/stalbot/domain/clock.py:127-130,264-296`
- Описание: регулярка `\d{2,4}` для года допускает и 3 цифры; докстринг обещает `None` при несовпадении формы. `parse_sheet_datetime("31.7.202 02:56")` → `datetime(202, 7, 31, ...)` вместо `None` — фидит `CacheSync`, опечатка в дате ячейки таблицы тихо создаёт мусорную дату в кэше/отчётах.
- Severity: medium.
- Кто выявил: code-reviewer (domain) через явный инвариант «отклонять неоднозначные/некорректные даты».
- Фикс: `(?P&lt;year&gt;\d{4}|\d{2})` — ровно 2 или 4 цифры.
- Regression-тест: да.
- Зависимости: нет.

**DOM-6** — `Ladder.progress()` не ограничивает `pct`/`done` снизу при отрицательном входе
- Файл: `src/stalbot/domain/progression/ladder.py:88-104`
- Описание: `pct = min(100, ...)` — только верхняя граница; отрицательный `value` даёт отрицательный `pct`. Ничего не гарантирует неотрицательность `xp`/`referrals_count` выше по цепочке.
- Severity: medium/low (защитный пробел, не подтверждённая достижимость от текущих вызывающих).
- Кто выявил: code-reviewer (domain), целевой edge-case запрос.
- Фикс: `max(0, min(100, ...))`.
- Regression-тест: да.
- Зависимости: смежно с DOM-7 (тот же участок кода).

**DOM-7** — `Ladder.progress()` может показать 100% на 1 единицу раньше реального достижения следующего тира
- Файл: там же, `ladder.py:88-104`
- Описание: `round(6999/7000*100) == 100`, хотя `next()` всё ещё не `None`.
- Severity: low (косметика, но реальная UX-нестыковка).
- Фикс: `pct = min(99, round(...))`, когда `upcoming is not None`.
- Regression-тест: да.
- Зависимости: чинить вместе с DOM-6.

**DOM-8 / ⚡ дублирует PRES-8** — `domain/errors.py::PermissionError` затеняет builtin, не используется нигде
- Файл: `src/stalbot/domain/errors.py:102-104`
- Severity: low/Nit (readability + dead code).
- Кто выявил: независимо code-reviewer (domain, DOM-8) и code-reviewer (presentation, PRES-8).
- Фикс: удалить, либо переименовать (напр. `AccessDeniedError`) и подключить с замапленным сообщением (см. PRES-1 — если используется, должен сразу попасть в `_DOMAIN_MESSAGES`).
- Regression-тест: нет.

**DOM-9** — (Optional) `Ladder` не валидирует «монотонно возрастающие пороги» при конструировании
- Файл: `progression/ladder.py:54-63`. Текущие данные корректны; латентный пробел на будущее (в отличие от `DateRange`, который валидирует себя в `__post_init__`).
- Severity: low/Optional. Regression-тест: нет (была бы новая проверка, не guard для существующего поведения).

**DOM-10** — (FYI) `progression/ranks.py`/`referrals.py` импортируют `config.ids` — зависимость домена на внешний слой
- Не баг; `config.ids` сам по себе pure-data, инвариант «zero I/O» не нарушен. Стоит осознанного решения (либо переносить id-карты в домен, либо явно документировать исключение).
- Severity: FYI.

---

## Application (`src/stalbot/application/**`)

**APP-1** — см. CLUSTER-1 в разделе «Критично».
**APP-2** — см. раздел «Критично».

**APP-3** — `/sync_prices`: непарсящаяся ячейка цены неотличима от пустой → тихое обнуление цены
- Файл: `src/stalbot/application/services/pricing.py:434-443` (`_cell_decimal`)
- Описание: `Decimal(str(value))` при `InvalidOperation` возвращает `None` без различения «пусто» / «мусор» (`#REF!`, локаль-форматированное число). `None != current_price` → генерируется реальный `PriceChange`, обнуляющий цену и на листе, и в кэше, репортится админу как обычное «изменение», а не как ошибка парсинга. `BoostOrderService.compute_total` пропускает строки с `price_sell is None` — обнулённая цена буста тихо выпадает из итога заказа.
- Severity: **high**.
- Кто выявил: code-reviewer (application) через `debugging-and-error-recovery` (симптом vs корневая причина).
- Фикс: различать «пустая ячейка» и «непарсящаяся ячейка» — вернуть sentinel/поднять исключение, вывести непарсящиеся ячейки отдельным списком в `SyncPricesReport` вместо молчаливого price-wipe.
- Regression-тест: да — ячейка с нечисловым мусором для уже имеющего цену предмета не должна менять цену, должна репортиться как ошибка.
- Зависимости: нет.

**APP-4** — `/del_item` перенумерация не синхронизирует `TicketSession.active_order_item_id`
- Файл: `application/services/catalog.py:100-147` vs `application/dto/ticket_session.py:37` / `services/tickets.py:177-185`
- Описание: `reassign_item_id` чинит `boost_order_lines`, но не активный указатель сессии тикета на выбранную позицию редактора — кнопки `+`/`-`/удалить тихо не срабатывают, пока игрок не переселектит позицию заново.
- Severity: medium.
- Фикс: массовое переприсвоение/очистка `active_order_item_id` для затронутых каналов при перенумерации.
- Regression-тест: да.
- Зависимости: нет.

**APP-5** — `CatalogService.add_item` вычисляет id/row из (возможно устаревшего) кэша, в отличие от `delete_item`, который явно перечитывает лист
- Файл: `application/services/catalog.py:82-98`
- Описание: два конкурентных `/item_add` при отстающем кэше могут вычислить одинаковый id/row → второй `write_verified` тихо перезапишет первый.
- Severity: medium.
- Фикс: либо перечитывать живой блок, как `delete_item`, либо сериализовать `add_item`/`delete_item` тем же локом, что и CLUSTER-1.
- Regression-тест: да.
- Зависимости: может делить лок с CLUSTER-1.

**APP-6** — N+1 обращений к локальному SQLite-кэшу в цикле (`boost_orders.py`, `stats.py`, `profile.py`, `pricing.py::apply_import`)
- Severity: low/Suggestion (производительность, не корректность; локальный SQLite, не сеть).
- Фикс: батч-метод `get_by_ids`/аналог в соответствующих репозиториях.
- Regression-тест: нет.

**APP-7** — `BoostOrderService.set_quantity` не клэмпит диапазон `MIN..MAX_QUANTITY`, в отличие от `adjust_quantity`
- Файл: `application/services/boost_orders.py:108-120`
- Severity: low/Suggestion — сейчас безопасно только потому, что единственный вызывающий уже валидирует.
- Фикс: клэмпить так же, как `adjust_quantity`.
- Regression-тест: да.

**APP-8** — `ScreenshotService.on_attached` не оборачивает `OcrGateway.recognize()` в try/except
- Файл: `application/services/screenshots.py:76`
- Описание: контракт «OCR никогда не блокирует подтверждение тикета» соблюдается только по конвенции текущей реализации (`NullOcrGateway`), не на уровне call site.
- Severity: low/FYI.
- Фикс: обернуть вызов, при исключении — деградировать в `status="failed"`.
- Regression-тест: да.

---

## Infrastructure: Sheets + Cache (`infrastructure/sheets/**`, `infrastructure/cache/**`)

**INFRA1-1**, **INFRA1-2** — см. раздел «Критично».

**INFRA1-3** — `retry_with_backoff` ловит только `gspread.exceptions.APIError`, не сетевые сбои
- Файл: `infrastructure/sheets/ratelimit.py:123-145`
- Описание: обрыв соединения/DNS/таймаут TLS — не `APIError`, уходит без ретрая и необёрнутым; выше по стеку не мапится в `SheetsUnavailableError`, попадает в generic «внутренняя ошибка» вместо дружелюбного «Sheets временно недоступен».
- Severity: **high** — пробел доступности на самом частом реальном классе сбоя.
- Фикс: ловить также сетевые исключения транспортного слоя, заворачивать в тот же retry/backoff и в `SheetsUnavailableError` при исчерпании.
- Regression-тест: да.

**INFRA1-4** — `_to_int` в `sync.py` тихо превращает непарсящиеся `coins`/`xp` в `0` (тот же класс бага, что уже был найден и исправлен для item id)
- Файл: `infrastructure/cache/sync.py:322-323,427-439`
- Severity: **medium-high** — тихая порча финансово значимого поля, тот же баг-класс, что уже официально задокументирован как исправленный в другом месте.
- Фикс: `_to_int` возвращает `int | None`; непарсящийся `coins`/`xp` — пропуск строки (как `amount`) либо явный warning-лог.
- Regression-тест: да.

**INFRA1-5** — `bool(purchase)`/`bool(sale)` наивно трактует любую непустую строку (включая `"FALSE"`) как `True`
- Файл: `infrastructure/cache/sync.py:264-287`
- Severity: medium — untrusted external data (ручная правка ячейки).
- Фикс: строгая коэрсия только настоящих `bool`/канонического набора строк.
- Regression-тест: да.

**INFRA1-6** — Write-lock не покрывает полную последовательность read-modify-write, только сам сетевой write-вызов
- Файл: `ratelimit.py:91-98`, `client.py:105-135`
- Описание: тот же корень, что и CLUSTER-1/SEC-3 — лок не даёт примитива, которым вызывающий код мог бы оградить весь read→compute→write.
- Severity: medium-high.
- Фикс: публичный `async with client.locked(...)`, используемый всеми read-modify-write路ями (включая `/del_item`, транзакции).
- Regression-тест: да, зависит от фикса CLUSTER-1.
- Зависимости: **делит фикс с CLUSTER-1**.

**INFRA1-7** — `_AcquireAll.__aenter__` не освобождает уже захваченные локи при отмене/исключении
- Файл: `infrastructure/sheets/client.py:349-361`
- Severity: low-medium — latent, не достижимо сегодня (все записи идут на один лист `DataBase`, `locks` всегда длины 1).
- Фикс: try/except с освобождением уже взятых локов перед re-raise.
- Regression-тест: да (нужна длина `locks` ≥ 2, сейчас недостижимо).

**INFRA1-8** — Docstring обещает инкрементальный синк транзакций, реализация каждый цикл перечитывает всю историю
- Файл: `infrastructure/cache/repositories/transactions.py:1-13` vs `sync.py:138-173`
- Severity: low/Optional (производительность, не корректность — upsert идемпотентен).
- Фикс: либо ограничивать чтение через `last_row()`, либо поправить докстринг.

**INFRA1-9** — `SCHEMA_VERSION`-миграции не выполняют реальный `ALTER TABLE`, только идемпотентный `CREATE TABLE IF NOT EXISTS`
- Файл: `infrastructure/cache/db.py:17-25,65-86`
- Severity: low/FYI — уже осознанно принято в комментарии кода (нет продовых данных для бэкфилла); упасть при первом реальном апгрейде громко, не тихая порча.
- Фикс: перед первым продовым релизом добавить настоящие `ALTER TABLE`-шаги, тестируемые против фикстур прежних версий схемы.

**INFRA1-10** — Write-lock ключуется по имени листа, не по блоку/диапазону — избыточная сериализация несвязанных записей
- Severity: low/Optional, информационно, не баг.

---

## Infrastructure: Discord / OCR / Logging / Config

**INFRA2-1** — `role_gateway.sync_roles()` ловит только `discord.Forbidden`, не `discord.HTTPException` целиком
- Файл: `infrastructure/discord/role_gateway.py:63-78`
- Описание: любой другой HTTP-сбой (удалённый ID роли, транзиентный 5xx) пробрасывается наружу и обрывает **весь** фоновый поллер прогрессии для всех оставшихся игроков — ровно то, что модуль обещает не допускать в своём же докстринге.
- Severity: **high**.
- Фикс: расширить `except` до `discord.HTTPException`, логировать проблемные role id, возвращать no-op `RoleDiff`.
- Regression-тест: да — плюс тест уровня `progression.sync()` с одним «плохим» профилем среди нескольких, остальные должны синкнуться.

**INFRA2-2** — Тот же try/except оборачивает grant и revoke вместе — частичный сбой врёт о том, что произошло
- Файл: там же, `role_gateway.py:63-78`
- Описание: если `add_roles` прошёл, а `remove_roles` упал — функция всё равно возвращает пустой `RoleDiff`, хотя игрок реально получил **и** новую, **и** старую роль (ломает взаимоисключение тиров).
- Severity: **high**.
- Фикс: разделить на два независимых try/except, `granted` отражает реально случившееся даже при сбое revoke.
- Regression-тест: да.
- Зависимости: тот же участок кода, что и INFRA2-1 — чинить вместе.

**INFRA2-3** — `trace_id` «залипает» навечно в долгоживущих фоновых `tasks.loop`
- Файл: `infrastructure/logging/trace.py` (используется, но `set_trace_id()` нигде не вызывается в проде)
- Описание: каждый `tasks.loop` (синк, поллер прогрессии, метрики) и воркер аудита живут в одном `asyncio.Task` весь процесс — первый лог фиксирует `trace_id` на всё время жизни процесса для этой петли, что противоречит цели механизма («один id = одна операция»).
- Severity: medium.
- Фикс: вызывать `set_trace_id(new_trace_id())` в начале тела каждой петли/итерации.
- Regression-тест: да.

**INFRA2-4** — `infrastructure/ocr/samples.py::save_sample()` не валидирует `extension`, латентный path traversal
- Файл: `infrastructure/ocr/samples.py:15-33`, источник — `application/services/screenshots.py:69`
- Описание: `extension` конкатенируется в путь без санитайза; сейчас недостижимо (единственный вызывающий передаёт константу `"screenshot.png"`), но граница доверия не валидируется на своём собственном уровне — естественное будущее изменение («сохранять реальное расширение загрузки») тихо откроет дыру.
- Severity: medium/Important (defense-in-depth, не активный эксплойт).
- Фикс: валидировать `extension` внутри `save_sample()` (например, `re.fullmatch(r"[A-Za-z0-9]{1,8}", extension)`).
- Regression-тест: да.

**INFRA2-5** — Сэмплы OCR всегда сохраняются с `.png`, независимо от реального формата (jpeg/webp)
- Severity: low/Suggestion (качество датасета, не баг поведения).
- Фикс: выводить расширение из `mime`/`content_type` через allowlist.

**INFRA2-6** — `Settings.log_level: str` не ограничен `Literal`, в отличие от `ocr_engine`
- Файл: `config/settings.py:55`
- Severity: low/Suggestion — опечатка в `.env` не ловится на стадии `pydantic.ValidationError`, падает позже с менее понятной ошибкой.
- Фикс: `Literal["DEBUG","INFO","WARNING","ERROR","CRITICAL"]`.
- Regression-тест: да.

**INFRA2-7** — Интервалы синка/поллера (`sync_users_interval_seconds` и др.) не ограничены `Field(gt=0)`
- Файл: `config/settings.py:39-41`
- Severity: low/Suggestion — `0`/отрицательное значение не отклоняется на старте, приведёт к «горячему» циклу против Discord/Sheets API.
- Фикс: `Field(gt=0)` на все три поля.
- Regression-тест: да.

**INFRA2-8** — Статус `"disabled"` OCR продублирован двумя независимыми константами без общего источника истины
- Severity: low/Suggestion (архитектура — повторяющийся литерал сигнализирует пропущенную абстракцию).
- Фикс: общий `Literal`/константа в `domain/entities/screenshot.py` или `application/ports/ocr.py`.

**INFRA2-9** — `configure_logging()` заменяет `root.handlers` без `.close()` предыдущих — латентная утечка файлового дескриптора при повторном вызове
- Severity: low/Nit — безвредно при текущем однократном вызове.
- Фикс: закрывать старые хендлеры перед переприсвоением.

---

## Presentation (non-ticket)

**PRES-1** — см. раздел «Критично».

**PRES-2** — `EmbedFactory.enforce_limits()` не гарантирует лимит 1024 символа на значение поля независимо от общей длины
- Файл: `presentation/embeds/factory.py:160-178`
- Описание: обрезка срабатывает только при превышении суммарных 6000 символов; поле с значением 1200 символов при небольшом общем размере embed пройдёт необрезанным и будет отклонено Discord API (`400 Bad Request`).
- Severity: **high** — именно тот граничный случай, который просили проверить.
- Фикс: безусловно клэмпить каждое `field.value`/`field.name` к 1024/256 до общей проверки.
- Regression-тест: да.

**PRES-3** — Цикл обрезки по общей длине в `enforce_limits()` потенциально не сходится (латентный DoS)
- Файл: `factory.py:171-176`
- Описание: при исчерпании значений полей до плейсхолдера `"—"` цикл не уменьшает имена полей и не ограничен по итерациям — если превышение вызвано именно именами полей/их количеством, цикл никогда не завершится (блокирующий бесконечный цикл на event loop). Не достижимо текущими вызывающими (только `audit()` использует `add_field` с короткими статичными именами), но это дефект в общей инфраструктуре.
- Severity: medium-high (латентно, не достигнуто сегодня).
- Фикс: уменьшать/отбрасывать имена полей, когда значения больше не сжимаются; либо ограничить число итераций.
- Regression-тест: да.

**PRES-4** — `on_timeout()` не обрабатывает `discord.NotFound`/`discord.Forbidden`, продублировано в 5 местах
- Файлы: `views/confirm.py:67-71`, `views/paginated_embed.py:77-83`, `views/paginated_select.py:85-91`, `views/logs_pager.py:117-123`, `cogs/catalog.py:249-255`
- Описание: `await self.message.edit(view=self)` без try/except — если сообщение удалено до таймаута, падает. Также структурное дублирование author-lock + message-bookkeeping в 5 независимых классах.
- Severity: medium.
- Фикс: обернуть в try/except; структурно — вынести общий базовый класс `AuthorLockedView`.
- Regression-тест: да (по одному на класс, либо один на общий базовый класс после рефакторинга).

**PRES-5** — `views/paginated_select.py::PaginatedItemSelect` — мёртвый код
- Описание: M10 реализовал собственный пикер вместо переиспользования этого компонента; нигде не используется.
- Severity: low/Suggestion — **спросить перед удалением**.
- Regression-тест: нет.

**PRES-6** — Autocomplete в catalog/pricing не проходит через runtime-проверку `admin_only()` (discord.py не гоняет `app_commands.check` для autocomplete)
- Severity: low/FYI — реальный барьер — permission-модель Discord (`default_member_permissions`), не эта проверка; каталог не содержит чувствительных данных.
- Regression-тест: нет.

**PRES-7** — Бизнес-правило валидации `/week` (диапазон ≤31 день, не в будущем) живёт в cog, а не в domain/application слое
- Severity: low/Suggestion (архитектура — логика вне своего слоя, непереиспользуема/нетестируема отдельно).
- Фикс: перенести в `DateRange.week()`/`StatsService.report()`.

**PRES-8** — см. DOM-8 (дубликат).

**PRES-9** — `bot.py::close()` отменяет фоновые петли и сразу закрывает `cache_db`, не дожидаясь их разворачивания
- Severity: low/FYI — правдоподобная, но не подтверждённая узкая гонка между отменой задачи в потоке-исполнителе и закрытием БД.
- Фикс (если подтвердится): собирать задачи петель и `await asyncio.gather(..., return_exceptions=True)` перед закрытием `cache_db`.
- Regression-тест: нет (потребовался бы гоночный интеграционный тест, вероятно того не стоит).

---

## Presentation: Ticket flow (`presentation/cogs/tickets/**`)

> **Не баг (подтверждено пользователем 2026-08-04):** хардкод «Ник: Scaryyyyy» в `cog.py` — это намеренный UI-плейсхолдер, не ошибка. Ни один агент его не репортил как баг — уже учтено в промптах.

**TICK-1** — см. CLUSTER-1 в разделе «Критично» (UI-сторона гонки подтверждения).

**TICK-2** — Ошибочно определённый автор тикета фиксируется навсегда, реальный автор теряет доступ к своему же заказу
- Файл: `cog.py:181-186` (`_on_start`), `648-659` (`_infer_author_id`)
- Описание: если `_infer_author_id` не смог определить автора по permission overwrites (вернул `0`), `author_id` навсегда становится первым, кто нажал «Заполнить заявку» — это может быть не открывший тикет игрок, а, например, персонал с доступом по роли. Далее это (а) неверно атрибутирует сделку не тому Discord-аккаунту, (б) навсегда блокирует реального автора в `_require_order_participant` (только автор-или-админ).
- Severity: **high** — прямое нарушение инварианта «не может остаться навсегда неверным», явно запрошенного к проверке.
- Фикс: не доверять «первому кликнувшему» как истине при неудачном инференсе; требовать явного подтверждения «это моя заявка» либо продолжать уточнять `author_id` до момента реальной отправки формы.
- Regression-тест: да.

**TICK-3** — Обработчики редактора заказа не проверяют `TicketStatus.CONFIRMED` (кроме кнопки подтверждения)
- Файл: `cog.py:301-419`
- Описание: после подтверждения сделки можно продолжать добавлять/удалять позиции черновика — редактор навсегда остаётся «редактируемым, но закрытым», подтвердить их снова уже нельзя (блокировано только на confirm), остаются осиротевшие строки черновика.
- Severity: medium.
- Фикс: добавить проверку статуса в начало каждого обработчика-мутатора, зеркалируя `_confirm_precheck`.
- Regression-тест: да.

**TICK-4** — Поле дедлайна при повторном открытии модалки после ошибки показывает `default` (старый неверный текст), из-за чего `placeholder` с текстом ошибки никогда не виден
- Файл: `modals.py:99-148`
- Описание: Discord показывает `placeholder` только у пустого поля; `default` перекрывает его — игрок видит свой же отклонённый текст без объяснения и обычно повторяет ту же ошибку.
- Severity: medium/important — ломает именно тот UX-путь round-trip, который просили проверить; существующий тест проверяет только факт вызова `send_modal`, не видимость ошибки.
- Фикс: не устанавливать `default` на поле дедлайна при переоткрытии после ошибки.
- Regression-тест: да.

**TICK-5** — Редактор заказа может накопить >25 позиций через постраничный выбор, но `discord.ui.Select` жёстко ограничен 25 опциями — краш рендера
- Файл: `order_views.py:92-99`, `cog.py:484-507`
- Описание: `BoostMultiSelectView` корректно пагинирует каталог по 25/страница, выбор сохраняется между страницами → можно набрать 26+ уникальных позиций в одном заказе. `_build_order_editor_view` строит один `Select` на все позиции без ограничения — 26-я позиция вызывает исключение discord.py при каждом последующем рендере этого редактора, необрабатываемое нигде в пути рендера.
- Severity: **high** — недостижимая после этого функциональность канала до вмешательства разработчика.
- Фикс: либо ограничить черновик 25 позициями на уровне `BoostOrderService`, либо пагинировать сам `_LineSelect`.
- Regression-тест: да.

**TICK-6** — Необработанное исключение, если канал тикета удалён во время/после ожидания Ticket Tool
- Файл: `cog.py:132-152,173-177`
- Описание: `_post_panel` вызывается без try/except; если канал удалён, `channel.send()` падает необработанным, сессия навсегда зависает в `AWAITING_TOOL`.
- Severity: medium/low.
- Фикс: обернуть `_post_panel` в try/except на `discord.HTTPException`.
- Regression-тест: да.

**TICK-7** — Гонка заполнения словаря `_tool_wait`: сообщение Ticket Tool может прийти до регистрации `asyncio.Event`
- Файл: `cog.py:141-161`
- Описание: деградирует до полного 30-секундного таймаута вместо мгновенной реакции — не крашится, просто медленнее.
- Severity: low.
- Regression-тест: опционально.

**TICK-8** — Нет проверки на самоприглашение (`referrer == сам игрок`) в формах тикетов
- Файл: `cog.py:216-226,263-274`
- Описание: в отличие от `/add` и `/set_referral` (проверено в других слоях), здесь такой guard отсутствует — требует явного продуктового решения о значимости эксплуатации.
- Severity: medium (pending product confirmation).
- Regression-тест: да, после подтверждения продуктового решения.

**TICK-9** — `BoostMultiSelectView._handle_change` выполняет полный send/edit публичного сообщения до подтверждения собственного interaction
- Severity: low/Suggestion — правдоподобно, не подтверждено воспроизведением; риск «This interaction failed» под сетевой задержкой.
- Regression-тест: опционально (сложно детерминированно проверить таймингом в unit-тестах).

**TICK-10** — Модалка количества молча усекает дробный ввод (`"9999.9"` → `9999`) вместо отказа
- Severity: low/Nit.
- Фикс: явно отклонять нецелые распарсенные значения перед `int(...)`.
- Regression-тест: да.

---

## Security — дополнительные находки (не вошедшие в кластеры выше)

**SEC-1** — `evaluate_amount` принимает нативный синтаксис scientific notation Python (`"1e400"`), даёт `Decimal('Infinity')`/`OverflowError`
- Файл: `domain/money.py:178-223`
- Описание: `_NUMBER_SPAN_RE` не распознаёт экспоненциальную нотацию, поэтому `ast.parse` видит её как обычный float-литерал — переполняется в `inf` без `SyntaxError`; whitelist AST пропускает `inf` (это легальный `float`). Только для админских команд (`/add`, `/setprice`, `/setboost`, подтверждение суммы) — не эксплуатируется рядовым игроком, но небрежный ввод админом роняет команду необработанным `OverflowError` вместо понятной ошибки валидации.
- Severity: medium.
- Кто выявил: `agent-skills:security-auditor`.
- Фикс: явно отклонять нефинитные результаты (`if not result.is_finite(): raise AmountParseError(...)`).
- Regression-тест: да — `"1e400"`, `"1e309"`, `"1e400 - 1e400"`, `"-1e400"` должны давать `AmountParseError`.
- Зависимости: тот же файл/функция, что DOM-1/DOM-3 — логично чинить всё evaluate_amount в одном заходе.

**SEC-4** — Системный пробел: ни один Modal в проекте не переопределяет `on_error`, поэтому будущие исключения в любой модалке падают тихо
- Обобщение корневой причины DOM-2/SEC-2 на весь `presentation/cogs/tickets/modals.py`.
- Severity: low (обобщение, не новый инцидент).
- Фикс: общий базовый класс/миксин `on_error`, маршрутизирующий через ту же `_resolve_message`/embed-конвенцию, что и `errors.py`.
- Regression-тест: не обязателен отдельно (закрывается тестом DOM-2/SEC-2); рекомендован один smoke-тест на видимость ошибки.

**SEC-5** — Нет per-user cooldown на «тяжёлых» админских командах (`/sync_prices`, `/new_price`)
- Severity: low — требует уже привилегированного (админ) аккаунта для эксплуатации; последствие — деградация задержки, не потеря данных.
- Фикс: `app_commands.checks.cooldown` как defense-in-depth, не срочно.

**SEC-6** — systemd unit можно защитить сильнее (доп. sandboxing-директивы)
- Присутствующее уже корректно (`NoNewPrivileges`, `ProtectSystem=strict`, `ProtectHome`, `PrivateTmp`, `ReadWritePaths`) — подтверждено, не просто заявлено.
- Отсутствует: `ProtectKernelModules`, `ProtectKernelTunables`, `ProtectControlGroups`, `RestrictSUIDSGID`, `RestrictNamespaces`, `LockPersonality`, `SystemCallFilter=@system-service`, `CapabilityBoundingSet=`.
- Severity: info — не блокирует, нет известного пути эксплуатации.

### Security — чисто, без замечаний (явно проверено)
Секреты (токен/ключ сервис-аккаунта нигде в git-истории, `.gitignore` корректен), SQL-инъекции (везде параметризовано), `eval`/`exec` в `evaluate_amount` (AST-whitelist корректно отклоняет `__import__`, атрибуты, вызовы, подписки, comprehensions, lambda, walrus), авторизация (`admin_only()` — оба слоя защиты на каждой админ-команде без исключений), CSRF/CORS (N/A — нет HTTP-сервера), Dockerfile non-root, docker-compose read-only mount креденшлов.

---

## Найдено при исправлении Этапа 0 (2026-08-05)

**INFRA1-11** — `TransactionService.register()` теперь сериализован одним общим локом
(фикс CLUSTER-1), но ни один Sheets-вызов внутри защищённого пути (`write_verified`,
`_await_formulas`'s `read_until`, `_extend_formulas`) не имеет таймаута — `retry_with_backoff`
реагирует только на пойманный `APIError`, зависшее TCP-соединение никогда его не бросит.
- Файлы: `application/services/transactions.py` (`register`/`_register_locked`),
  `infrastructure/sheets/client.py` (`_ensure_open`/`batch_get`/`batch_update` — все идут
  через `asyncio.to_thread` без `timeout=`), `infrastructure/sheets/ratelimit.py`
  (`retry_with_backoff`).
- Описание: до фикса CLUSTER-1 зависший сетевой вызов блокировал только вызвавший его
  запрос. После фикса он блокирует ЛОК на весь процесс — один завис­ший `/add` или
  подтверждение тикета останавливает вообще все админ-действия с деньгами до рестарта
  бота. Рост blast radius — прямое следствие сделанного в Этапе 0 фикса, подтверждено
  `security-auditor` (severity: medium) при ревью CLUSTER-1.
- Severity: **medium** (self-inflicted DoS на core money-path, не эксплуатируется
  извне, но триггерится обычным сетевым сбоем).
- Предлагаемый фикс: `asyncio.wait_for(...)` вокруг Sheets-вызовов внутри залоченного
  пути и/или таймаут на HTTP-сессии `gspread`-клиента.
- Regression-тест нужен: да — зависший `write_verified` не должен блокировать процесс
  бесконечно.
- Зависимости: делит корень с INFRA1-3 (тот же пробел — `retry_with_backoff` не ловит
  сетевые сбои вне `APIError`); логично чинить в одном заходе с Этапом 3.

**TICK-11** — ✅ **исправлено в Этапе 0** (изначально найдено `security-auditor` при
ревью CLUSTER-1, отложено, затем `agent-skills:code-reviewer` независимо поднял тот же
вопрос при обязательном ревью этапа и оценил как блокирующий — исправлено сразу, не
откладывалось на будущий этап).
`TransactionRegistrationResult` не различал «свежая запись» / «идемпотентный реплей»,
из-за чего повторное подтверждение тикета (проигравший конкурентный вызов после фикса
CLUSTER-1/TICK-1) дублировало видимые пользователю пост-подтверждные side-effects.
- Файл: `application/dto/transaction_request.py` (поле `replayed: bool`),
  `application/services/transactions.py::_replay_if_seen`,
  `presentation/cogs/tickets/cog.py::_on_amount_submitted`.
- Фикс: `TransactionRegistrationResult.replayed` (`True` только из `_replay_if_seen`);
  cog при `result.replayed` вызывает `record_confirmed` (идемпотентно) и показывает
  «уже зафиксирована», но пропускает `screenshots.record_confirmed_amount`,
  `boost_orders.clear`, `progression.sync` и публичное сообщение в канал.
- Regression-тест: `tests/unit/presentation/cogs/tickets/test_cog.py::
  test_amount_submitted_skips_duplicate_side_effects_on_a_replayed_registration`.

## Найдено при исправлении Этапа 2 (2026-08-05)

**INFRA1-12** — новые батч-методы `get_by_ids`/`get_by_nicks`/`get_nick_displays`
(добавлены для APP-6) строят `WHERE id/nick_norm IN (...)` с неограниченным числом
`?`-плейсхолдеров — количество зависит от размера входного списка.
- Файлы: `infrastructure/cache/repositories/items.py::get_by_ids`,
  `infrastructure/cache/repositories/users.py::get_by_nicks`/`get_nick_displays`.
- Описание: у SQLite есть предел на число параметров в одном запросе
  (`SQLITE_MAX_VARIABLE_NUMBER`, 999 на старых сборках / 32766 на актуальных, собранных
  с недавним Python). Ни один сегодняшний вызывающий не подходит близко к пределу
  (черновик буста одного канала, один прайс-импорт, различные игроки одного периода,
  рефералы одного игрока) — но ничего не документирует и не защищает от превышения,
  если в будущем один из вызовов получит на вход многотысячный список.
- Severity: low/FYI (недостижимо сегодняшними вызывающими, но не задокументировано).
- Кто выявил: `agent-skills:code-reviewer` при обязательном ревью Этапа 2.
- Предлагаемый фикс: либо чанковать запрос (батчами по N id/nick), либо явно
  задокументировать предположение о размере входа в докстринге метода.
- Regression-тест: нет (нужен только если добавляется чанкинг).
- Зависимости: нет.

---

## Найдено при исправлении Этапа 4 (2026-08-05)

**INFRA2-10** — `ProgressionService._sync_one` не различает игроков внутри одного тика
фонового поллера трейс-айдишником
- Файл: `application/services/progression.py:108-143` (`sync`/`_sync_one`),
  `presentation/bot.py::_run_progression_poll` (фикс INFRA2-3).
- Описание: INFRA2-3 поставил `set_trace_id(new_trace_id())` в начало каждого тика
  `_run_progression_poll` — теперь тик как целое отличим от других тиков в логах. Но
  `ProgressionService.sync(nicks=None)` внутри одного тика перебирает **всю базу игроков**,
  вызывая `DiscordRoleGateway.sync_roles()` отдельно на каждого — все эти отдельные
  ролевые sync-вызовы внутри одного тика делят один и тот же trace_id. Для трёх других
  циклов (`_run_users_sync`/`_run_items_sync`/`_run_metrics_log`) весь тик и есть одна
  логическая операция — для этого цикла нет.
- Severity: low/Suggestion (несостыковка гранулярности, не порча данных).
- Кто выявил: `agent-skills:code-reviewer` при обязательном ревью Этапа 4.
- Предлагаемый фикс: `set_trace_id(new_trace_id())` внутри `_sync_one`, на каждого игрока,
  а не (или в дополнение к) на весь тик.
- Regression-тест: да, если будет реализовано.
- Зависимости: делит контекст с INFRA2-3.

**INFRA2-11** — уточнённый `RoleDiff` (фикс INFRA2-2) по-прежнему нигде не используется
вызывающим кодом
- Файл: `application/services/progression.py:143` — `await self._roles.sync_roles(...)`,
  результат отбрасывается.
- Описание: `DiscordRoleGateway.sync_roles()` (Этап 4, INFRA2-2) теперь корректно
  репортит частичный сбой (`granted=(X,), revoked=()`, если revoke упал после успешного
  grant) вместо ложного «ничего не изменилось». Но единственный вызывающий,
  `ProgressionService._sync_one`, никак не использует возвращённый `RoleDiff` — ни
  логирования частичного сбоя, ни алерта админу. Точность фикса INFRA2-2 сейчас
  наблюдаема только в тестах `role_gateway.py`, не в проде.
- Severity: low/Suggestion (нет потери данных — частичный сбой уже логируется внутри
  `sync_roles` через `logger.warning`; это про наблюдаемость на уровне выше).
- Кто выявил: `agent-skills:code-reviewer` при обязательном ревью Этапа 4.
- Предлагаемый фикс: требует продуктового решения — что делать при частичном сбое
  (просто залогировать на уровне `ProgressionService`? отправить в аудит-канал? ретраить?).
  Не чинить без этого решения.
- Regression-тест: нет (нужен только после решения, что именно делать).
- Зависимости: нет.

---

## Test-coverage — сигналы для увязки с regression-тестами выше

(Полная детализация — см. отчёт `agent-skills:test-engineer`; сведено здесь как быстрые сигналы «покрыто / не покрыто» для соответствующих находок.)

| # | Область | Статус | Влияет на |
|---|---|---|---|
| TEST-1 | Настоящая конкурентность идемпотентности | **не покрыто** | CLUSTER-1 |
| TEST-2 | Исчерпывающий тест защищённых диапазонов | ✅ покрыто | подтверждает INFRA1-1 не имеет соседних пробелов |
| TEST-3 | Контрактный тест порогов формул | ✅ покрыто | — |
| TEST-4 | `SheetsClient` под реальным `APIError` через полный путь | частично | INFRA1-3 |
| TEST-5 | Автор-или-админ на **всех** обработчиках редактора заказа | частично (3/5) | TICK-3, качественно смежно с TICK-2 |
| TEST-6 | Побайтовая идентичность поведения при выключенном OCR | **не покрыто как контракт** | APP-8, INFRA2-8 |
| TEST-7 | Переживание рестарта persistent views | **не покрыто** | TICK-2 (архитектурно корректно, но не гарантировано тестом) |
| TEST-8 | Точность refill-математики rate limiter'а | частично (только грубое поведение) | INFRA1-6 |
| TEST-9 | DoS-guard'ы `evaluate_amount` (длина/глубина/степень) | ✅ покрыто, без точной границы off-by-one | SEC-1, DOM-1 |
| TEST-10 | `tests/integration/` фактически пуст — 0% формальных интеграционных тестов | флаг структуры | все находки, завязанные на реальное поведение Sheets API |
