services:
  stalbot:
    build: .
    image: stalbot:latest
    container_name: stalbot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./credentials:/app/credentials:ro
