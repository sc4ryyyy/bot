# UX/Bug — прогресс выполнения

Отслеживание статуса по [uxbug.md](uxbug.md). Обновлено: 2026-08-06 (все пункты закрыты).

Легенда: ✅ Done · 🟡 Partial · ❌ Not done

| # | Задача | Статус | Комментарий |
|---|--------|--------|-------------|
| 1 | Редактор заказа: итоговый эмбед + кнопка редактирования, "Завершить заказ" для админов | ✅ Done | Флоу переделан: форма → итоговый эмбед `OrderSummaryView` ("✏️ Редактировать", доступно автору/админу) → `OrderEditorView` (кнопка переименована в "✅ Подтвердить", доступна автору/админу, возвращает к итоговому эмбеду) → "🏁 Завершить заказ" (только админ, открывает `AmountModal` и регистрирует сделку — то, что раньше делала кнопка редактора напрямую). `order_card.py`/`order_views.py`/`cog.py`. Коммит `a496d04`. |
| 2 | Продажа предметов: удалять скриншот из чата, чинить таблицу/сообщение о сделке | ✅ Done | Таблица и сообщение о завершении сделки уже работали (механизм из CLUSTER-1). Скриншот теперь удаляется из чата (`message.delete()`, try/except `discord.HTTPException`) после закрепления в эмбеде/архиве. `tickets/cog.py`. Коммит `0b0b2da`. |
| 3 | `/price_list`: столбцы, без "-", эмодзи бустов | ✅ Done | Каждый предмет — отдельное inline-поле эмбеда (Discord рендерит их колонками); отсутствующая цена скупки/продажи просто опускается вместо "—". Эмодзи буста уже был. `catalog.py`. Коммит `9508d4d`. |
| 4 | `/tag`: информация о сервере | ✅ Done | DM теперь включает название сервера (`interaction.guild.name`). `tag.py`. Коммит `3bb6e6b`. |
| 5 | `/profile`: разделение полей | ✅ Done | Coins/XP/Ранг/Реф-роль/Приглашено — отдельные поля эмбеда (`embed.add_field`) вместо одной склеенной строки. `profile.py`. Коммит `85db17b`. |
| 6 | Кликабельные ники в логах | ✅ Done | Единственное оставшееся место с голым текстом (`factory.py`'s audit-эмбед) теперь использует `<@id>`-упоминание; везде в остальном коде (`stats.py`, `card.py`, `transactions.py`, `profile.py`) упоминания уже были. Коммит `98d60f9`. |
| 7 | `/sync_price`: направление синхронизации и диапазоны | ✅ Done | Подтверждено с пользователем перед изменением: item database — источник истины, `/sync_prices` теперь пишет цены ИЗ базы НА листы (было наоборот). Диапазоны исправлены: Мейн скуп с 5 строки, БУСТЫ с 4, Скуп бустов с 3 (конец диапазонов оставлен как был, по явному решению). Эмбед показывает diff через тот же рендерер, что `/setprice`/`/new_price`. `layouts.py`/`pricing.py` (application+presentation). Коммит `6af6e1d`. |
| 8 | Заглавные буквы в аргументах команд | ✅ Done | Было готово ещё до этой сессии — все `describe()`/`Choice(name=...)` уже с заглавной буквы. |
| 9 | Оформление эмбед-сообщений | ✅ Done | Автор эмбеда/футер теперь контекстные ("Логи" для audit-эмбеда, "Заявки" для тикетов) вместо всегда "Stalzone"; футер — "Клондайк Шёпота \| <контекст> • дата". Остальная часть запроса ("не слипшийся текст") закрыта через #3/#5/#14. `embeds/factory.py`, `embeds/palette.py`. Коммиты `98d60f9`, `711d8fe`. |
| 10 | Убрать `=` и кавычки из логов | ✅ Done | `"id=%s"`/`"trace=%s"`/`"code=%s"` → `"id %s"`/`"trace %s"`/`"code %s"` в `bot.py`/`errors.py`/`ratelimit.py`. JSON-конверт файлового/stdout-логгера намеренно не тронут — это транспортный формат, а не текст сообщения. Коммит `8142528`. |
| 11 | `/day`, `/week`, `/month`: сделки с датами | ✅ Done | `PeriodReport` теперь несёт `deals: tuple[TransactionRecord, ...]`; после агрегированных страниц добавляется хронологический список "🧾 Сделки" с датой/ником/типом/суммой каждой сделки. `stats.py` (application+presentation). Коммит `02f76e6`. |
| 12 | Подтверждение прикрепления скриншота | ✅ Done | После обработки скриншота(ов) отправляется transient-подтверждение (`delete_after=8.0`, упоминание отправителя) — ближайший аналог "эфемерности" вне контекста interaction. `tickets/cog.py`. Коммит `0b0b2da`. |
| 13 | Несколько скриншотов | ✅ Done | Все изображения в сообщении архивируются в лог-канал и проходят через `ScreenshotService.on_attached`; в карточке заявки по-прежнему показывается только первое (ограничение Discord — один `image` на эмбед). `tickets/cog.py`. Коммит `0b0b2da`. |
| 14 | Двоеточия после пунктов в заявках | ✅ Done | `card.py`: "Игрок:", "Игровой ник:", "Способ:", "Пригласил:", "Создана:". `order_card.py` уже был с двоеточиями. Коммит `b789bd1`. |
| 15 | Скриншоты без кнопки "прикрепить" не должны попадать в датасет/логи | ✅ Done | Новое состояние `_awaiting_screenshot` (per-channel), выставляется при нажатии "📸 Прикрепить скриншот", проверяется в `on_message` до какой-либо обработки изображения; сбрасывается только после успешной записи скриншота. `tickets/cog.py`. Коммит `0b0b2da`. |

## Сводка

- ✅ Done: 15 из 15
- 🟡 Partial: 0
- ❌ Not done: 0

Все пункты `uxbug.md` реализованы, прогнаны через `agent-skills:code-reviewer` (по каждой смысловой группе — `/tag`+`/profile`+эмбеды, каталог/статистика, тикеты-скриншоты, редактор заказа, `/sync_prices`), с фиксами по важным находкам ревью (переупорядочен OCR-путь в скриншотах, исправлен edge-case двойного репорта в `/sync_prices`). Полный прогон тестов — 1023 unit-теста зелёные, `mypy`/`ruff` чисты по всем затронутым файлам.

## Как обновлять

При закрытии пункта — заменить статус на ✅ Done, дописать комментарий (что именно сделано, коммит/PR), и обновить сводку внизу.
